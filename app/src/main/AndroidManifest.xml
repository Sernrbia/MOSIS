<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mosis_ispit">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.bluetooth" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_PRIVILEGED"
        tools:ignore="ProtectedPermissions" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mosis_ispit"
        tools:ignore="AllowBackup">
        <activity android:name=".secondscreen.view.InDiscussionJoin"
            android:label="InDiscussionJoin"
            android:theme="@style/AppTheme.ActionBar" />
        <activity android:name=".secondscreen.view.InDiscussion"
            android:label="InDiscussion"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".secondscreen.view.MainScreenActivity"
            android:label="@string/titleActivityMainScreen"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".secondscreen.view.ProfileViewActivity"
            android:label="ProfileView"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".secondscreen.view.CreateDiscussion"
            android:label="CreateDiscussion"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".firstscreen.view.LogInActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.ActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>